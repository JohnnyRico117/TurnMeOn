name: turn_me_on
description: A new Flutter application.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# Read more about versioning at semver.org.
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:

  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.2
  duration: ^2.0.4
  firebase_auth: ^0.8.1+4
  google_sign_in: ^4.0.1+1
  cloud_firestore: ^0.9.5+2
  firebase_storage: ^2.1.0+1
  image_picker: ^0.5.2


dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  uses-material-design: true

  assets:
    - assets/brooke-lark-385507-unsplash.jpg
    - assets/g-logo.png
    - assets/birthday-christmas-gift-17796.jpg
    - assets/caique-silva-481033-unsplash.jpg
    - assets/No_picture_available.png

  fonts:
    - family: Merriweather
      fonts:
        - asset: fonts/Merriweather-Regular.ttf
    - family: Roboto
      fonts:
        - asset: fonts/Roboto-Medium.ttf